# Flask Configuration
FLASK_SECRET_KEY=your-secret-key-here-generate-a-random-string
FLASK_ENV=development

# Database Configuration
DATABASE_PATH=data/inventory.db

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit the .env file to version control
# 4. Generate a secure secret key using: python -c "import secrets; print(secrets.token_hex(32))"
